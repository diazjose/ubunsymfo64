{% use 'bootstrap_5_layout.html.twig' %}

{% block form_errors %}
  {% if errors|length > 0 %}
    <ul class="list-unstyled text-danger small">
      {% for error in errors %}
        <li>{{ error.message }}</li>
      {% endfor %}
    </ul>
  {% endif %}
{% endblock %}
