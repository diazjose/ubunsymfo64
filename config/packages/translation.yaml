framework:
    default_locale: es #agregado
    #default_locale: en 
    translator:
        default_path: '%kernel.project_dir%/translations'
        providers:
        fallbacks:      #agregado
            - es        #agregado